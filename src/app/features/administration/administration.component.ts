import{Component,inject,signal}from'@angular/core';import{RouterLink}from'@angular/router';import{UsersService,InstitutionsService,DirectoryService}from'../../core/services/api.services';import{forkJoin}from'rxjs';
@Component({standalone:true,imports:[RouterLink],template:`<div class="page-head"><div><span class="eyebrow">CONFIGURACIÓN</span><h1>Administración</h1><p>Gestión exclusiva para el rol ADMIN.</p></div></div>@if(loading()){<section class="state-card">Cargando recursos administrativos…</section>}@else if(error()){<section class="state-card error">{{error()}}</section>}@else{<div class="admin-grid"><article><span>♙</span><h2>Usuarios</h2><strong>{{counts().users}}</strong><p>Cuentas, roles y alcance institucional.</p><a class="secondary button" routerLink="/administracion/usuarios">Gestionar usuarios</a></article><article><span>⌂</span><h2>Instituciones</h2><strong>{{counts().institutions}}</strong><p>Instituciones habilitadas en Manizales.</p><a class="secondary button" routerLink="/administracion/instituciones">Gestionar instituciones</a></article><article><span>⌘</span><h2>Directorio</h2><strong>{{counts().services}}</strong><p>Servicios y datos operativos actualizables.</p><a class="secondary button" routerLink="/directorio">Gestionar directorio</a></article><article><span>◎</span><h2>Auditoría</h2><p>Consulta de trazabilidad sin información clínica innecesaria.</p><a class="secondary button" routerLink="/auditoria">Consultar auditoría</a></article></div>}`})export class AdministrationComponent{private users=inject(UsersService);private institutions=inject(InstitutionsService);private directory=inject(DirectoryService);loading=signal(true);error=signal('');counts=signal({users:0,institutions:0,services:0});constructor(){forkJoin({u:this.users.list(),i:this.institutions.list(),d:this.directory.list()}).subscribe({next:r=>{this.counts.set({users:r.u.users.length,institutions:r.i.institutions.length,services:r.d.services.length});this.loading.set(false)},error:()=>{this.error.set('No fue posible cargar la administración.');this.loading.set(false)}})}}
